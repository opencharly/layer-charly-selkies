# 2026.243.1016 — docs: stop pinning a CalVer in skill usage examples

## Summary

A usage example inside this repo's skill document (`content:` block) pinned a hardcoded
CalVer. That version is guaranteed to rot: it cannot be kept current without minting a
release for every documentation edit, and while stale it teaches a version nobody should
copy. It now reads `:vTAG`, which never goes stale and fails fast if pasted literally
rather than silently building against an old pin.

That matters here specifically because these blocks are **skill documents read by agents**,
where a stale-but-valid version is the more dangerous of the two failure modes.

The ref changed in THIS diff:

```
  @github.com/opencharly/pod-dbus:v2026.235.2115
            becomes  @github.com/opencharly/pod-dbus:vTAG
```

The candy's own CalVer advances from `2026.218.1200` to `2026.243.0909` because the skill content changed,
and changed content must ship under a new version rather than reuse a released one.

```
 charly.yml | 10 +++++-----
 1 file changed, 5 insertions(+), 5 deletions(-)
```

## The complete diff

Every changed line, pasted rather than described:

```diff
-        version: 2026.218.1200
+        version: 2026.243.0909
-                - '@github.com/opencharly/pod-dbus:v2026.235.2115'
+                - '@github.com/opencharly/pod-dbus:vTAG'
-                - '@github.com/opencharly/pod-dbus:v2026.235.2115'
+                - '@github.com/opencharly/pod-dbus:vTAG'
-                - '@github.com/opencharly/pod-dbus:v2026.235.2115'
+                - '@github.com/opencharly/pod-dbus:vTAG'
-                - '@github.com/opencharly/pod-dbus:v2026.235.2115'
+                - '@github.com/opencharly/pod-dbus:vTAG'
```

Nothing else changes: no packages, services, plan steps, or dependency edges.

## Proof these refs are prose, not dependency edges

This is the load-bearing claim, so it is shown rather than asserted. Each changed ref sits
inside a block scalar opened by a `key: |` line, which YAML treats as opaque text:

```
  ref at line 215:  - '@github.com/opencharly/pod-dbus:vTAG'
    enclosed by  line 190: content: |-   <- block scalar; YAML treats all of it as TEXT
    and by       line 208: ```yaml            <- a fenced example INSIDE that text
    decisive: lines between 190 and 215 with indent <= 8 (which would CLOSE the block): 0
    => the block never closes before the ref; it is text, never resolved

  ref at line 301:  - '@github.com/opencharly/pod-dbus:vTAG'
    enclosed by  line 278: content: |-   <- block scalar; YAML treats all of it as TEXT
    and by       line 293: ```yaml            <- a fenced example INSIDE that text
    decisive: lines between 278 and 301 with indent <= 8 (which would CLOSE the block): 0
    => the block never closes before the ref; it is text, never resolved

  ref at line 370:  - '@github.com/opencharly/pod-dbus:vTAG'
    enclosed by  line 356: content: |-   <- block scalar; YAML treats all of it as TEXT
    and by       line 363: ```yaml            <- a fenced example INSIDE that text
    decisive: lines between 356 and 370 with indent <= 8 (which would CLOSE the block): 0
    => the block never closes before the ref; it is text, never resolved

  ref at line 794:  - '@github.com/opencharly/pod-dbus:vTAG'
    enclosed by  line 770: content: |-   <- block scalar; YAML treats all of it as TEXT
    and by       line 783: ```yaml            <- a fenced example INSIDE that text
    decisive: lines between 770 and 794 with indent <= 8 (which would CLOSE the block): 0
    => the block never closes before the ref; it is text, never resolved```

charly parses `content:`/`description:` as strings; it never resolves a ref inside one. The
mechanical confirmation is the resolving-ref count below, which is byte-identical across the
change.

**On `git diff`'s hunk header.** The `@@ ... @@` suffix on these hunks names a key that looks
structural (e.g. `deploy-skill:`), and it is tempting to read that as proof the ref is a real
dependency entry. It is not: that suffix is git's *context heuristic* — the nearest preceding
line that is less indented and matches its pattern. Inside a block scalar every line is opaque
text, so the heuristic latches onto whatever the prose happens to contain. The two checks above
read the file's actual structure; the hunk header does not.

Anyone can verify independently — the enclosing `key: |` opener and the fence line are quoted
above with their line numbers, so `sed -n '<opener>,<ref>p' charly.yml` shows the whole block.

## Empirical proof — charly's own behaviour, with a negative control

Structural argument aside, the authoritative test is what charly does.  is not a
resolvable tag, so if these refs were dependency edges, validation would FAIL on them.

| # | tree | result |
|---|---|---|
| A | this repo, unmodified |  exit **0** |
| B | this repo, the changed refs at  (i.e. THIS PR) | exit **0** |
| C1 | control: , unmodified (5 real resolving refs) | exit **0** |
| C2 | control: , a REAL ref set to  | exit **1** |

C2's failure is the control that gives B its meaning — it proves charly does resolve and does
reject an unresolvable tag:

```
$ sed -i "s|layer-ffmpeg:v2026.239.1614|layer-ffmpeg:vTAG|g" charly.yml
$ charly box validate ; echo "exit=0"
charly: error: command "validate": validation error: downloading
  github.com/opencharly/layer-ffmpeg:vTAG: InvokeProvider refs:refs op=resolve: resolving ...
exit=1
```

So charly fails hard on an unresolvable REAL ref (C2) and does not notice these ones (B).
That is only possible if it never resolves them — which is the claim, demonstrated rather
than argued.

## How tested

**The set of refs this file RESOLVES is unchanged** — 0 before, 0 after. Only prose
moved. This is the property that matters, because a doc example and a real pin are
indistinguishable by shape (both render as `  - '@github.com/opencharly/x:vY'`), so the
rewrite is block-scalar aware and re-parses the file after writing, refusing to save if the
resolving set differs.

`charly box validate` on the **final committed tree**, with the `v2026.238.1242` binary this repo pins:

```
$ charly box validate
$ echo $?
0
```

Exit code read from charly directly, never through a pipe — a piped `| tail` reports the
pipe's status rather than the command's.

### Context (NOT part of this diff)

This is one repo in a 23-repo documentation sweep. The scanner and rewrite tooling that found
and made this change live outside this repository and are **not** in this diff — this diff is
only the `charly.yml` lines shown above. The sweep exists because an org-wide pin scan kept
re-flagging these repos: it counted refs inside `content:`/`description:` block scalars as
dependency edges, when charly never resolves them. They were never part of the
`resolved to multiple versions` problem, but could never be current either.

## Rulebook compliance

- **R1 (RCA)** — traced from a sweep that would not converge to prose being counted as
  dependency edges, and to the rotting CalVer that made prose permanently stale.
- **R3 (no duplication)** — one placeholder convention applied identically across the sweep.
- **R4 (no workarounds)** — the alternative was rewriting prose on every wave forever, minting
  a release for documentation churn.
- **R5 (delete legacy)** — the hardcoded CalVer is replaced, not left beside the placeholder.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** — validated on the final committed tree, exit code above, with
  the resolving-ref count asserted equal across the change.

## Change classification

- **Change class:** documentation — a skill usage example, plus the candy's own CalVer stamp.
  No dependency edges change; the resolving-ref set is asserted identical.
- **Verification gate:** `charly box validate` exit 0 on the committed tree, plus the
  resolving-ref equality assertion
- **Attribution tier:** `documentation reviewed`, and a runtime tier would be a POLICY
  VIOLATION here, not merely unjustified. The rulebook is explicit: *"Runtime confidence is
  forbidden for prose-only work"*, and `documentation reviewed` covers *"documentation,
  comment-only edits"* and is *"forbidden the moment code/config behavior is touched."*
  Nothing behavioral is touched: no packages, services, or plan steps, and the
  resolving-ref set is asserted byte-identical, so no dependency edge moves. The criterion
  is WHAT changed, not the file's extension — prose living inside `charly.yml` is still
  prose, as the proof section above shows line by line. The only non-prose line is the
  candy's own CalVer stamp, which is a release label the changed-content rule requires,
  not behavioral configuration. The `charly box validate` exit-0 run is reported as the
  non-runtime standard passing, never as grounds for a runtime tier.

*Assisted-by: Claude Code Anthropic Opus 5 (documentation reviewed)*