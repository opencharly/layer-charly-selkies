# 2026.265.1929 — refactor: drop the moved family skills from layer-charly-selkies

## Summary

Remove the 5 skill entities from this repo whose artefacts have moved to their owning repo. They now live as a `skill:` entity in the distro-* owner repos (the producer PRs merged and tagged them) and are generated into the marketplace from there. Keeping a second copy here is an R3 one-owner violation.

**Files in the diff, all accounted for:**

- `charly.yml` — deletes `selkies-kde-nvidia, selkies-kde, selkies-labwc-nvidia, selkies-labwc, sway-browser-vnc` and bumps the schema stamp (`version:`) from `2026.232.0520` to `2026.249.2125`.
- `CHANGELOG/2026.265.1657.md` — this repo's per-release history file (standing convention).

## How tested

Gate: `charly box validate` (the config-class gate for a skill-entity deletion).

**1. `charly box validate` on this branch (post-deletion), with a NEGATIVE CONTROL
proving it genuinely loads/validates `skill:` entities** — breaking the `name:`
field of a REMAINING skill in this same tree makes validate FAIL, naming the node:

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors

$ # negative control: delete `name:` from the remaining `selkies-desktop-layer-skill`
$ charly box validate
charly: error: command "validate": validation error: …/charly.yml:doc0: node "selkies-desktop-layer-skill":
  plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.name: incomplete value =~"^[a-z][a-z0-9-]*$"
```

- **Disposable target:** N/A — config class; no image/deploy created or changed.
- **Fresh-rebuild confirmation:** N/A — config class; this repo builds no binary.

**2. Successor liveness — each deleted entity is present on its owner's pinned tag,
with its `family:` unchanged (real, executed output):**

```
$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -n 'name: selkies-kde-nvidia$'
92:        name: selkies-kde-nvidia

$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -n 'name: selkies-kde$'
3:        name: selkies-kde

$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -n 'name: selkies-labwc-nvidia$'
585:        name: selkies-labwc-nvidia

$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -n 'name: selkies-labwc$'
170:        name: selkies-labwc

$ git -C distro-fedora show v2026.265.1656:charly.yml | grep -n 'name: sway-browser-vnc$'
1846:        name: sway-browser-vnc
```

**2b. The moved entity's `family:`/`owner:` at the pinned tag** (this is what makes
the surviving `/charly-<family>:<skill>` reference still resolve):

```
$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -A2 'name: selkies-kde-nvidia$'
name: selkies-kde-nvidia
        family: selkies
        owner: selkies-kde-nvidia

$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -A2 'name: selkies-kde$'
name: selkies-kde
        family: selkies
        owner: selkies-kde

$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -A2 'name: selkies-labwc-nvidia$'
name: selkies-labwc-nvidia
        family: selkies
        owner: selkies-labwc-nvidia

$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-selkies-skills/charly.yml | grep -A2 'name: selkies-labwc$'
name: selkies-labwc
        family: selkies
        owner: selkies-labwc

$ git -C distro-fedora show v2026.265.1656:charly.yml | grep -A2 'name: sway-browser-vnc$'
name: sway-browser-vnc
        family: selkies
        owner: sway-browser-vnc
```

**3. Repo-wide sweep — EXACT command and COMPLETE output.** Every surviving hit is
reconciled in the table (which lists only hits actually present above):

```
$ grep -rn -E 'selkies-kde-nvidia|selkies-kde|selkies-labwc-nvidia|selkies-labwc|sway-browser-vnc' . --exclude-dir=.git
./charly.yml:120:            - `/charly-selkies:selkies-labwc`
./charly.yml:121:            - `/charly-selkies:selkies-labwc-nvidia`
./charly.yml:155:            See `/charly-selkies:selkies-labwc` for full multi-instance deployment examples.
./charly.yml:167:            - `/charly-selkies:selkies-labwc` — CPU labwc box that bundles this metalayer (with `/charly-selkies:selkies-labwc-nvidia` for the GPU build)
./charly.yml:263:            - `/charly-selkies:sway-browser-vnc` (via `sway-desktop` metalayer)
./charly.yml:264:            - `/charly-selkies:selkies-labwc` (via `selkies-desktop` metalayer)
./charly.yml:265:            - `/charly-selkies:selkies-labwc-nvidia` (via `selkies-desktop` metalayer)
./CHANGELOG/2026.265.1657.md:9:- `selkies-kde-nvidia-skill:`
./CHANGELOG/2026.265.1657.md:10:- `selkies-kde-skill:`
./CHANGELOG/2026.265.1657.md:11:- `selkies-labwc-nvidia-skill:`
./CHANGELOG/2026.265.1657.md:12:- `selkies-labwc-skill:`
./CHANGELOG/2026.265.1657.md:13:- `sway-browser-vnc-skill:`
# exit=0
```

| surviving hit | kind | disposition |
|---|---|---|
| `charly.yml` line(s) | a `/charly-<family>:<skill>` cross-reference (see the namespace note) | **kept** — the family is unchanged, so the reference still resolves |
| `CHANGELOG/2026.265.1657.md` | this PR's own entry naming what it deleted | **kept** — it is the history file |

**Namespace note.** A skill's canonical reference is `/charly-<family>:<skill>`,
where `<family>` is the skill's `family:` field (required on every `skill:`
entity; `candy/plugin-marketplace` `emit_skills.go:28` routes a skill to
`plugins/<familyName>/skills/<name>`, and each plugin's `name` is
`charly-<family>`). Proof 2b shows the moved entity's `family:` is unchanged, so
the reference still resolves.

**4. The generation closure carries these skills from their new owners — the
cross-repo R10 proof.** `opencharly/marketplace#325` (MERGED) ran the generator
over a closure with the nine aggregator sources REMOVED and their moved skills
deleted (git `insteadOf` to locally-patched copies) — the exact post-deletion
state — and all 37 wave skills still emitted from the distro-* pins:

```
$ charly marketplace generate --root proj-closure --out closure-out3
EXIT=0
wave skills present in closure-WITHOUT-aggregators: 37/37
missing: []
content-differs: []
```

**5. Schema-stamp bump — the real `charly migrate` output.** `version:` is
`2026.249.2125`, which is what `charly migrate` writes (proven by running it on a
clean copy of `origin/main`):

```
$ sed -n '1p' charly.yml
version: 2026.232.0520
$ charly migrate
migrated to schema 2026.249.2125
$ sed -n '1p' charly.yml
version: 2026.249.2125
```

## Wave / landing order

1. Producer PRs — MERGED + tagged.
2. `opencharly/marketplace#325` — pins the owner repos (MERGED).
3. **this PR** — activation (the deleted copies are the last duplicates).

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — the deletion is the counterpart of a completed relocation; no divergence.
- **R2** — one coherent wave; this is its activation step.
- **R3** — one owner per skill after this landing.
- **R4** — no sleeps/retries/workarounds; `charly box validate` is the gate.
- **R5** — deprecated copies deleted (not shimmed); the repo-wide sweep is the self-test.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — validate (with negative control), successor liveness, and the sweep ran (real output above).
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class; proof 4's generation run is the cross-repo check; proof 1 is the in-repo gate.

## Change classification

Config (skills entity deletion) → `analysed on a live system`. Proof: owners carry every deleted entity with its family (pasted output); validate clean + negative control; repo-wide sweep reconciled; generation closure run proves the corpus is unaffected.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*